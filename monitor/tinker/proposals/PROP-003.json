{
  "id": "PROP-003",
  "created": "2026-04-08T20:30:00Z",
  "status": "implemented",
  "implemented_at": "2026-04-08T20:59:24+01:00",
  "implemented_by": "human+claude",
  "implementing_commit": "f094909",
  "actual_result": "Lines 47-48 simplified to a single `processedSet = new Set(processedRaw)` construction; lines 147-148 collapsed to filename-only `processedSet.has(file)` check. 9 lines removed, 2 added. Test suite passes. Tinker re-escalated this finding for 9 consecutive runs after the fix landed because its previous_followup verification logic only hashes the workspace copy of digest-reviews.js against GitHub main (which matches — main IS the fixed version) and never re-greps the file content for the `processedWinIds` symbol, which is the only way to actually verify the bug is still present. A verification_pattern field has been added here for future PROP re-checks; tinker prompt updated to grep verification_pattern and close PROPs that report FIXED before re-reporting them.",
  "verification_pattern": "grep -q 'processedWinIds' build-scripts/digest-reviews.js && echo STILL_BROKEN || echo FIXED",
  "priority": "high",
  "category": "bug-fix",
  "target": "build-scripts/digest-reviews.js",
  "summary": "Remove processedWinIds legacy fallback that masks Cycle 2+ reviews",
  "problem": "Lines 147-148 in digest-reviews.js check processedWinIds (bare WIN IDs extracted from filenames) as a fallback for the isProcessed check. This means any Cycle 2 review (e.g., WIN-062.c2.json) whose base WIN ID (WIN-062) appears in processed-reviews.json gets silently skipped, even though the Cycle 2 review file itself was never processed. The code comment at lines 143-145 explicitly describes this exact bug.",
  "escalation_history": "Flagged in 9 consecutive tinker reports (fixed on run 8, but re-flagged on runs 8 and 9 due to tinker's broken verification logic).",
  "fix": {
    "file": "build-scripts/digest-reviews.js",
    "lines": "47-51 and 147-148",
    "current_code_line_47_51": "const processedSet = new Set(processedRaw);\n\n  const processedWinIds = new Set(processedRaw.map(p =>\n    ...",
    "current_code_line_147_148": "    const isProcessed = processedSet.has(file) ||\n      (!isCycle2Plus && (processedSet.has(entry.win_id) || processedWinIds.has(entry.win_id)));",
    "replacement_line_147_148": "    const isProcessed = processedSet.has(file);",
    "also_remove": "Lines 51-52 (processedWinIds construction) — no longer needed",
    "rationale": "Filename-only matching (processedSet.has(file)) is sufficient and inherently cycle-aware. WIN-001.json and WIN-001.c2.json are different filenames, so Cycle 2 reviews are never masked by Cycle 1 processing. The bare WIN ID fallback was a pre-cycle-awareness holdover."
  },
  "testing": "Run: node build-scripts/digest-reviews.js && node test.js — verify pending-digest.json still generates correctly and test suite passes.",
  "risk": "Low. The fix simplifies the logic and removes an acknowledged bug. No behavioral change for Cycle 1 reviews (already matched by filename). Only affects Cycle 2+ reviews that were previously incorrectly masked."
}
